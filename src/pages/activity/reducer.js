const initialState = {
    position: [0, 0],
};

const activiteReducer = (state=initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default activiteReducer;